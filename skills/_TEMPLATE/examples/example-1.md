# Example 1 — <what this demonstrates>

> Replace this stub. Every craft skill ships ≥1 worked example here (see `CONTRIBUTING.md`).
> A worked example is a real applied artifact, NOT prose: an applied spec (type scale with
> px/rem, token JSON, OKLCH ramp), a before/after with reasoning, a sample artifact (@font-face,
> state matrix, grid template, DOCX style table), or a worked decision (stated typeface + colour
> + reason for a named brief). Never lorem/placeholder — specific and reusable.

## Context
<the brief / artifact this example applies to>

## The worked artifact
<the actual spec / code / table / before-after>

## Why it's non-slop
<the deliberate, human-design reasoning — ties to the Mission and doctrine>
